[package]
name = "matrixclaw-manifests"
version = "0.1.0"
edition = "2021"

[lib]
name = "matrixclaw_manifests"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
