[package]
name = "zstar-compat-openclaw"
version = "0.1.0"
edition = "2021"

[lib]
name = "zstar_compat_openclaw"
path = "src/lib.rs"

[dependencies]
zstar-session-runtime = { path = "../session-runtime" }
serde = { version = "1", features = ["derive"] }
