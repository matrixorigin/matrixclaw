[package]
name = "matrixclaw-compat-openclaw"
version = "0.1.0"
edition = "2021"

[lib]
name = "matrixclaw_compat_openclaw"
path = "src/lib.rs"

[dependencies]
matrixclaw-session-runtime = { path = "../session-runtime" }
