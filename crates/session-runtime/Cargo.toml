[package]
name = "zstar-session-runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "zstar_session_runtime"
path = "src/lib.rs"

[dependencies]
zstar-agent-core = { path = "../agent-core" }
rusqlite = { version = "0.31", features = ["bundled"] }

[dev-dependencies]
tempfile = "3"
