[package]
name = "matrixclaw-session-runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "matrixclaw_session_runtime"
path = "src/lib.rs"

[dependencies]
matrixclaw-agent-core = { path = "../agent-core" }
rusqlite = { version = "0.31", features = ["bundled"] }

[dev-dependencies]
tempfile = "3"
